name: Backend CI Smoke

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  backend-ci-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Docker backend smoke path
        run: make api-docker-smoke
